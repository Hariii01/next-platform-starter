<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Classic Login</title>
  <style>
    /* ---------- Color Palette ---------- */
    :root {
      --black: #0b0b0d;
      --skyblue: #8ecae6;
      --pink: #eaa6c3;
      --gold: #e6d7b9;
      --text: #f0f0f0;
      --muted: #9ca3af;
      --radius: 14px;
      --shadow: 0 6px 24px rgba(0,0,0,0.6);
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      background: radial-gradient(circle at top left, rgba(142,202,230,0.08), transparent 50%),
                  radial-gradient(circle at bottom right, rgba(234,166,195,0.08), transparent 50%),
                  var(--black);
      color: var(--text);
    }

    .login-box {
      width: 360px;
      padding: 36px;
      background: rgba(255,255,255,0.04);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
    }

    .login-box h2 {
      margin: 0 0 22px 0;
      text-align: center;
      color: var(--gold);
    }

    .form-group {
      margin-bottom: 18px;
    }

    label {
      font-size: 14px;
      color: var(--gold);
      display: block;
      margin-bottom: 6px;
    }

    input {
      width: 100%;
      padding: 12px;
      border: 1px solid rgba(255,255,255,0.15);
      border-radius: var(--radius);
      background: rgba(0,0,0,0.4);
      color: var(--text);
      font-size: 15px;
    }

    input:focus {
      outline: none;
      border-color: var(--skyblue);
      box-shadow: 0 0 0 2px rgba(142,202,230,0.3);
    }

    .btn {
      width: 100%;
      padding: 12px;
      margin-top: 10px;
      border: none;
      border-radius: var(--radius);
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      background: linear-gradient(90deg, var(--pink), var(--skyblue));
      color: var(--black);
      transition: transform 0.15s ease;
    }

    .btn:hover {
      transform: translateY(-2px);
    }

    .links {
      margin-top: 18px;
      text-align: center;
      font-size: 14px;
      color: var(--muted);
    }

    .links a {
      color: var(--skyblue);
      text-decoration: none;
    }

    .links a:hover {
      text-decoration: underline;
      color: var(--gold);
    }
  </style>
</head>
<body>
  <div class="login-box">
    <h2>Login</h2>
    <form>
      <div class="form-group">
        <label for="username">Username</label>
        <input type="text" id="username" placeholder="Enter your username" required>
      </div>
      <div class="form-group">
        <label for="password">Password</label>
        <input type="password" id="password" placeholder="Enter your password" required>
      </div>
      <button type="submit" class="btn">Sign In</button>
    </form>
    <div class="links">
      <p><a href="#">Forgot Password?</a></p>
      <p>Don’t have an account? <a href="#">Register</a></p>
    </div>
  </div>
</body>
</html>
